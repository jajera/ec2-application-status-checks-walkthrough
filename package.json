{
  "name": "ec2-application-status-checks-walkthrough",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run generate:og-image && astro build",
    "generate:og-image": "tsx scripts/generate-og-image.ts",
    "preview": "astro preview",
    "validate": "npx tsx scripts/validate-content.ts",
    "format": "prettier --write .",
    "lint": "markdownlint-cli2 \"src/**/*.mdx\"",
    "test": "vitest --run"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.7",
    "astro": "^7.2.4",
    "sharp": "^0.35.3",
    "starlight-base-path": "^0.2.1",
    "starlight-theme-vintage": "^0.1.0"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "gray-matter": "^4.0.3",
    "markdownlint-cli2": "^0.23.2",
    "prettier": "^3.5.3",
    "tsx": "^4.19.4",
    "vitest": "^4.1.11"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
